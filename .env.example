# Pfad zur Firebase-Service-Account-JSON für die scripts/ (Export aus
# Claude-Artefakten, Sync Markdown <-> Firestore). Nur serverseitig genutzt,
# nie im Browser. Datei selbst NICHT in dieses Repo legen (siehe .gitignore).
GOOGLE_APPLICATION_CREDENTIALS=/absoluter/pfad/serviceAccount.json
